server:
  port: 18080

spring:
  application:
    name: mypet-frontend-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

mypet:
  db:
    host: 127.0.0.1
    port: 1800
    user: postgres
    password: mypg123abc

jwt:
  secret: mypet-jwt-secret-key-2026-minimum-32chars!!
  expiration: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    app.xinqianmao.com: DEBUG
    root: WARN
