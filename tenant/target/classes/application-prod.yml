# 生产环境配置
mypet:
  db:
    host: ${DB_HOST}
    port: ${DB_PORT}
    user: ${DB_USER}
    password: ${DB_PASSWORD}
  base-url: ${BASE_URL}

logging:
  level:
    app.xinqianmao.com: WARN
    root: WARN
